export const SORT_ORDERS = Object.freeze({
  ASC: 'asc',
  DESC: 'desc',
} as const);

export const FILTER_TYPES = Object.freeze({
  SEARCH: 'SEARCH',
  SELECT: 'SELECT',
  CUSTOM: 'CUSTOM',
} as const);